#include "lists.h"

/**
* find_listint_loop - finds the loop in a linked list
* @head: pointer to the first node
*
* Return: the address to the start node in the loop, NULL if !exist
*/

listint_t find_listint_loop(listint_t *head)
{
	size_t len = 0; int j

}
